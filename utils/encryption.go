package utils

func HashPassword(password string) (string, error) {
	//generate hash from password and return
	return "", nil
}

func ValidatePassword(hashedPassword, password string) error {
	//compare hashed password with password
	return nil
}
